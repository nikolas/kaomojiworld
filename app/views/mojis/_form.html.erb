<%= form_for(@moji) do |f| %>
  <% if @moji.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@moji.errors.count, "error") %> prohibited this moji from being saved:</h2>

      <ul>
      <% @moji.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :content %><br />
    <%= f.text_field :content %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
